import type { ExtensionAPI, ExtensionFactory } from "@mariozechner/pi-coding-agent";

const extensionFactory: ExtensionFactory = (pi: ExtensionAPI) => {
  // todo: implement ynab tools here
};

export default extensionFactory;
